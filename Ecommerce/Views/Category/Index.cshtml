@model IEnumerable<Category> 
@{
    ViewData["Title"] = "Index";
}

@if (Model.Count() > 0)
{
    <div class="d-flex justify-content-between align-items-center">
        <h1>Categories</h1>
        <a asp-action="Create" class="btn btn-outline-success"><i class="bi bi-plus-circle"></i> Create Categorie</a>
    </div>
    <table class="table table-bordered table-striped">
        <tr>
            <th>Category ID</th>
            <th>Category Name</th>
            <th>Edit and Delete</th>
        </tr>
    
        @foreach (var item in Model)
        {

            <tr>

                <td>@item.CategoryId</td>
                <td>@item.CategoryName</td>
                <td>
                    <a asp-action="Update" asp-route-id="@item.CategoryId" class="btn btn-info mx-2"> Edit </a>
                    <a asp-action="Delete" asp-route-id="@item.CategoryId" class="btn btn-danger"> Delete </a>
                </td>
            </tr>
        }

    </table>
}   
else{
    <div class="d-flex justify-content-between">
        <div class="text-danger">there is no Categories</div>
        <a asp-action="Create" class="btn btn-outline-success"><i class="bi bi-plus-circle"></i> Create Categorie</a>

    </div>
}
